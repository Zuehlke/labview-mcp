# Tool catalogue

Every tool the server exposes, and which of them are safe to allow-list. A test asserts the
counts on this page against the registration in the source, so they cannot drift. It is called
`ToolCatalogueTests`.

## What is served

**83 tools over 23 RPCs.** Forty-four map to no RPC: `lvai_status`, `lvai_dump_schema`,
`lvai_palette_index`, `lvai_example_index`, `lvai_set_vi_icon`, `lvai_render_diagrams` and `lvai_discuss_file` — which compose three RPCs
rather than wrapping one — `lvai_check_aixml`, which reads an AIXML file, `lvai_describe_class` and `lvai_describe_ctl`, which read a `.lvclass`
and a `.ctl` off disk and need no LabVIEW at all, the knowledge tools below, and the five `pylv_*`
tools. 33 carry `readOnlyHint`,
25 carry `destructiveHint`, so a client can gate the writes.

The server also exposes its five embedded documents as **MCP resources** —
`labview://aixml-reference`, `labview://dqmh-patterns`, `labview://lvproj-structure`,
`labview://lvlib-lvclass-structure` and `labview://vi-server-reference` — for clients that read
resources rather than call tools.

### Read — safe

| Tool | RPC |
|---|---|
| `lvai_status` | — (discovery + health + reflection) |
| `lvai_describe_ctl` | — (reads the `.ctl` with pylabview; no LabVIEW) |
| `lvai_dump_schema` | — (server reflection) |
| `lvai_aixml_reference` | — (embedded [AIXML reference](../aixml-reference.md)) |
| `lvai_dqmh_reference` | — (embedded [DQMH reference](../dqmh-patterns.md)) |
| `lvai_lvproj_reference` | — (embedded [.lvproj reference](../lvproj-structure.md)) |
| `lvai_lvlib_reference` | — (embedded [.lvlib/.lvclass reference](../lvlib-lvclass-structure.md)) |
| `lvai_vi_server_reference` | — (embedded [VI Server catalogue](../vi-server-reference.md), queried row-wise) |
| `lvai_palette_index` | — (scans the installed LabVIEW's `menus\*.mnu`) |
| `lvai_connector_pane` | — (composes `ValidateAIXML` + `ConvertAIXMLToVI` + `RunVIAsTopLevel` + `ConvertVIToAIXML`, plus the embedded [pattern table](../connector-pane-patterns.tsv)) |
| `lvai_get_application_configuration` | `GetApplicationConfiguration` |
| `lvai_describe_vi` | `GetDescribeVIPromptInfo` |
| `lvai_describe_project` | `GetDescribeProjectPromptInfo` |
| `lvai_search_info_cache` | `SearchInfoCache` |
| `lvai_lookup_info_cache_items` | `LookupInfoCacheItems` |
| `lvai_filter_palette_search_candidates` | `FilterPaletteSearchCandidates` |
| `lvai_filter_example_search_candidates` | `FilterExampleSearchCandidates` |
| `lvai_convert_vi_to_aixml` | `ConvertVIToAIXML` |
| `lvai_convert_vis_to_aixml` | — (batches `ConvertVIToAIXML`; cached exports come back concurrently, uncached ones queue through LabVIEW) |
| `lvai_check_aixml` | — (pure text analysis; **needs no LabVIEW**) | **pre-flight check for the three faults `ValidateAIXML` was MEASURED to accept**: a `uid_parent` naming no element, which LabVIEW silently reparents to the top-level diagram so a node meant to sit inside a loop ends up outside it; a duplicate `uid`, silently renumbered so the export stops matching your file; and a Ring default outside its `values`. Deliberately does NOT check terminal names, types, wiring or cycles - LabVIEW does those well, and a second source of truth would drift |
| `lvai_validate_aixml` | `ValidateAIXML` |
| `lvai_vi_terminals` | — (composes `ConvertVIToAIXML` and reads the pane) |
| `lvai_describe_class` | — (parses the `.lvclass` XML directly; **needs no LabVIEW**, so it is the one honest reading after a timeout) |
| `lvai_coercion_dots` | — (composes `ValidateAIXML` + `ConvertAIXMLToVI` + VI Server reads) — every subVI call terminal with its `Coercion Dot?`. The placeholder route leaves one on each terminal whose real subVI carries a typedef, and validation, the retarget and a run all pass in that state; this is the only thing that sees it. Needs no active project |
| `lvai_exec_state` | — (composes `ValidateAIXML` + `ConvertAIXMLToVI` + VI Server reads) — a VI's `Execution:State` and `VI Linker Errors`, so "is this executable?" is answerable WITHOUT running it. That matters because a top-level UI loop never ends on its own. `0` is eBad; `-1` means the reference never opened, which is kept apart on purpose so it cannot read as broken. Needs no active project |
| `lvai_list_labview_installations` | — (reads the installed versions off the machine) |

### Write — mutating

| Tool | RPC | What it changes |
|---|---|---|
| `lvai_convert_aixml_to_vi` | `ConvertAIXMLToVI` | **creates/overwrites a `.vi`** |
| `lvai_apply_aixml_to_vi` | `ApplyAIXMLToVI` | **CLOSED** — refuses unless `userAskedForThisByName`. It has never patched a VI from a third-party client, and since 2026-09-16 it says so with a silent `errorCode 0` instead of `Error 42`. Edit by regenerating |
| `lvai_run_vi_as_top_level` | `RunVIAsTopLevel` | **executes code** (hardware, files, …) |
| `lvai_set_vi_icon` | — (composes `ValidateAIXML` + `ConvertAIXMLToVI` + `RunVIAsTopLevel`) | **replaces a `.vi`'s icon** and saves it in place |
| `lvai_render_diagrams` | — (same composition) | **renders block diagrams to PNG**, several VIs in one call, and reports the images per VI - top-level diagram first, then one per Case frame. Creates the image directory, which LabVIEW does not (`Error 118`). The only check that sees a clipped or occluded diagram comment; measured saving about 100 s of a 1000 s run against driving the print helper by hand |
| `lvai_close_vi` | — (same composition) | closes a VI in the IDE, **releasing it from memory** |
| `lvai_close_active_project` | — (same composition) | **saves** the active project and closes it |
| `lvai_add_vis_to_project` | — (closes the project, then a file edit) | **edits a `.lvproj`**: lists VIs under one folder, never twice - moves a target-level entry into the folder and removes any file listed twice (`Error 74` on open) |
| `lvai_build_from_build_specification` | `BuildFromBuildSpecification` | writes build output |
| `lvai_open_file` | `OpenFile` | IDE state |
| `lvai_find_palette_item` | `FindPaletteItem` | IDE state |
| `lvai_drop_palette_item` | `DropPaletteItem` | edits a block diagram |
| `lvai_log_usage_data` | `LogUsageData` | writes telemetry |
| `lvai_generate_vi` | — (composes `ValidateAIXML` + `ConvertAIXMLToVI` + the pane measurement) | **creates a `.vi`** and reports a connector pane that breaches the style guide |
| `lvai_make_malleable` | — (composes `lvai_generate_vi` + `pylv_extract` + a four-flag LVSR patch + `pylv_rebuild` + `lvai_exec_state`) | **creates a MALLEABLE VI (`.vim`)**, which AIXML cannot do alone: `ConvertAIXMLToVI` accepts a `.vim` path, answers `errorCode 0` and writes a VI that is `execState 0` while check, validate, convert and the pane measurement are all green — measured with NI's own VIM as the control, where the same diagram is eIdle as a `.vi` and eBad as a `.vim`, and a pure file copy to a `.vim` name breaks too. LabVIEW decides malleability from the EXTENSION at load and then needs the VI configured for inlining, which is a VI property AIXML cannot write. So this generates an ordinary `.vi` first, **gates on it being executable** — a diagram broken as a `.vi` is rescued by nothing below — then sets `ShouldInline`, `InlineStg`, `DebugCapable` and `SaveParallel`, rebuilds, and reads `execState` back, because that is the only cheap check that sees this failure. It does NOT close the project (a close SAVES LabVIEW's copy over the `.lvproj`), so when the target already existed it reports `staleReadRisk` rather than silently reading the copy LabVIEW still holds |
| `lvai_run_vi_and_read_values` | — (same composition plus VI Server reads) | **executes code**, then reads every control and indicator back — use it whenever an output is not a string |
| `lvai_discuss_file` | — (composes `ValidateAIXML` + `ConvertAIXMLToVI` + `RunVIAsTopLevel`) | **puts NI's assistant into Discuss mode** on a `.vi` or `.lvproj`, launching the Nigel chat if it is not running — the IDE's "Discuss with Nigel" in its own two stages, `Launch Nigel Chat.vi` then `InvokeDiscussVI.vi`, driven over VI Server **by qualified name**. It does that rather than calling NI's menu callback — which is reachable as an AIXML `Call` and was the first route this shipped with — because the callback **returns nothing**, discarding InvokeDiscussVI's own error code; `discussErrorCode` is that discarded verdict and `ok` is gated on it. Measured: 181 ms against 667 ms through the callback. It is the EMITTING side; `lvai_monitor_discuss_vi` still cannot receive the event, and this does **not** unlock `lvai_apply_aixml_to_vi` — that is still `Error 42`. The extension is checked before the call, because the chat rejects a non-VI in its own window where no caller can read it |
| `lvai_wire_dynamic_events` | — (composes `ValidateAIXML` + `ConvertAIXMLToVI` + `RunVIAsTopLevel`) | **branches an event registration refnum onto an Event Structure's DYNAMIC EVENT terminal** — the one connection AIXML cannot express, and therefore the only thing a regeneration loses once the refnum is authored into the structure as an ordinary `<Tunnel>`, which AIXML does keep. Without it a VI with user-event frames is `Execution:State` **0, broken**; with it, 1 — measured as a controlled pair. Both ends are found **by name** (`Event Registration Refnum`, which comes from the data type), and the net's source is walked to, because offering the structure's own tunnel is `Error 1062` — two sinks have no direction. `Auto Route?` is wired TRUE, and that is the whole difference between a wire and a ghost: at its FALSE default the connection is present in the signal's terminal list, the terminal's flag bit, the wire table, a live `Connected Wire` and `Is Broken?` — and LabVIEW draws **nothing**, with `Position` and `Bounds` identical either way. So `ok` is gated on the wire's END COUNT going 2 → 3, and the answer tells you to verify with `lvai_render_diagrams` **from a copy at a path LabVIEW has never loaded** |
| `lvai_generate_vi_with_events` | — (composes `ConvertAIXMLToVI` + `pylv_extract` + two helper scripts + `pylv_rebuild` + `lvai_exec_state`) | **creates a `.vi` whose Event Structure is actually REGISTERED**, which `lvai_generate_vi` cannot: it skips validation on purpose, because `ValidateAIXML` refuses every static event frame while `ConvertAIXMLToVI` on the same bytes writes the VI. Conversion keeps the frames and collapses `EventNodeEvents`, so this writes one EventSpec per frame plus the cached frame label — omitting that label leaves the IDE showing an event case with no event while an export, a render and `execState` are all green. No mapping argument: the selectors carry the control and the trigger. An unknown trigger and a dynamic user event are refused by name |
| `lvai_set_event_data_fields` | — (composes `pylv_extract` + two helper scripts + `pylv_rebuild` + `lvai_exec_state`) | **points an Event Structure frame's Event Data Node at the PAYLOAD of the user event it handles** — the default shape of a user event, and the one thing generation cannot produce: LabVIEW's importer discards the field selection, because at conversion time the frame carries no registration and only `Source`, `Type` and `Time` exist. Third call of the route, after `lvai_generate_vi_with_events` and `lvai_wire_dynamic_events`. It REPURPOSES rows LabVIEW already made and MOVES an existing wire — nothing is composed, because inventing a `compressedWireTable` is what killed LabVIEW when this repository last added heap objects. So the AIXML must wire a labelled placeholder constant into a **primitive or subVI input**: a front-panel terminal and a tunnel carry no type, and the row takes its type from the sink. Field index **4 is the first payload item** — 0 Source, 1 Type, 2 Time, 3 UsrEvtRef — and two per frame is the ceiling, because the `Source` row carries no index to rewrite. A constant placeholder is KEPT (deleting one gives `load error code 6, Could not load block diagram`); a node placeholder is deleted, and the helper decides which from what it finds. Verifies on a COPY, because LabVIEW is still serving its own in-memory copy of the path just rebuilt |
| `lvai_placeholder_subvi` | — (composes `ConvertVIToAIXML` + `ValidateAIXML` + `ConvertAIXMLToVI`) | **writes one folder into the LabVIEW installation** — a placeholder whose connector pane clones the VI you name, so a generated VI can be given a `Call` that `pylv_apply {"op":"retarget"}` then points at your own code. Cached by signature; uninstall by deleting `user.lib\LV_MCP` |
| `lvai_bind_typedef_constants` | — (same composition plus VI Server writes) | **re-points block diagram constants** onto the typedef their subVI terminal expects, removing the coercion dot the placeholder route leaves behind. You supply no `.ctl` path — `Create Constant` on the terminal yields the exact type, and `Replace` is what preserves the wire. Finds each constant by its **label**, so author them as `_name="<terminal name>"`. Needs a project open and active |
| `lvai_bind_pane_typedef` | — (composes `ValidateAIXML` + `ConvertAIXMLToVI` + VI Server writes + `pylv_extract`) | **installs a `.ctl` typedef onto a CLASS MEMBER’s own front-panel controls** — the other half of `lvai_bind_typedef_constants`, and the half nothing else reaches. A generated VI cannot carry a typedef on its own pane (AIXML has no typedef in its grammar, and `lvai_add_class_method` retypes only the CLASS terminals), so a call into a subVI whose terminal IS the typedef wears a coercion dot that survives the flatten and the swap. The constants tool finds its target by a constant’s **label** and cannot reach a control. The owning `.lvclass` is REQUIRED: measured on a plain VI, `Save.Instrument` alone answered error 0 at every stage and the saved file carried zero typedef objects. The verdict is read from the SAVED FILE, never from the helper’s own count. Needs a project open and active |
| `lvai_resave_ctl` | — (composes `ValidateAIXML` + `ConvertAIXMLToVI` + `Save.Instrument` + `pylv_extract`) | **makes LabVIEW write a flag-patched `.ctl` in its own shape.** A `.ctl` built the fixture way — generate a VI to a `.ctl` path, then patch the flags with pylabview — has never been written by LabVIEW and still carries that VI’s **connector pane**; `lvai_describe_ctl` shows it as `wrappedType: "Function"` and flags `needsLabviewSave`. It binds and flattens fine, and NI’s accessor wizard then refuses the field it is bound to with `Error 1061`. One `Save.Instrument` with the path unwired fixes it. Needs a project open and active |
| `lvai_flatten_typedefs` | — (composes `pylv_extract` + `pylv_rebuild` + VI Server writes) | **gives a placeholder stub the subject's real terminal types with no typedef link at any depth** — each `.ctl` of the chain is copied into `user.lib\LV_MCP`, its `TypeDefVI` flag is cleared with pylabview, and the copy is installed with `{LV.Control} Replace`, which against a non-typedef installs the type and creates no link. NOT `Discon Typedef`: that call kills LabVIEW from a generated helper, measured five times. The recursion is mandatory — clearing the outer flag alone lets an embedded typedef through. A class terminal is deliberately left alone. `lvai_placeholder_subvi` runs it for you |
| `lvai_generate_test` | — (composes the placeholder, `lvai_generate_vi` and the retarget) | **creates a Caraya unit-test `.vi`** that calls your VI as an ordinary static subVI, one node per case. Eighteen hand calls before this existed, ten of them editing an object heap |
| `lvai_generate_class_test` | — (composes `lvai_generate_vis`, `lvai_generate_vi` and `lvai_swap_subvis`) | **creates a Caraya round-trip test for a CLASS**, one write-then-read per field, every accessor an ordinary static subVI. `lvai_generate_test` cannot reach class code — its placeholder is generated through AIXML, which refuses a class-typed terminal — so this authors `path` sockets and lets LabVIEW's own `Replace` re-type the wires. About forty hand calls before it existed |
| `lvai_generate_caraya_test_runner` | — (composes `lvai_generate_vi`) | **creates the ONE VI that runs a whole Caraya suite** — every test's path built relative to the runner's own location, so the folder stays movable, `Interactive (T)` FALSE and a `.xml` report path. Hand-authoring it cost 186 s of wall clock against 6.1 s inside LabVIEW, measured over a five-suite build |
| `lvai_swap_subvis` | — (composes `ConvertAIXMLToVI` + `RunVIAsTopLevel` + `ConvertVIToAIXML`) | **repoints many subVI nodes and class constants on one diagram** through LabVIEW's own `Replace`, in a single run. Driving it from outside cost 19 calls for one suite, because `SubVIs[]` re-orders after every swap and the replaced reference dies. Nodes first, constants last; verifies against LabVIEW's own export TWICE - no socket name left in it, and no call node carrying fewer wired terminals than it went in with. The second check exists because a swap between panes of the SAME type re-attached a value wire onto the class wire, and a targets-only verify called that a correct restore |
| `lvai_lunit_add_test_method` | — (composes `ConvertAIXMLToVI` + `pylv_apply` + `RunVIAsTopLevel` + `ConvertVIToAIXML`) | **finishes authored AIXML into LUnit test methods of a test case class**, many in one call: converts *without* validating — the validator refuses a class wire the generator accepts — forces the pane onto LUnit's 4815 pattern, retypes the class terminals, adds each VI as a class MEMBER, and verifies from LabVIEW's own export. Membership goes last, because a finished member cannot be safely re-converted. Six methods cost 85 hand calls before it existed |
| `lvai_run_lunit_tests` | — (composes `ConvertAIXMLToVI` + `RunVIAsTopLevel`) | **runs an LUnit suite and returns the report already parsed**, per case, with each failure's own message. Closes the trap that LUnit never overwrites a report but writes `name (1).xml` beside it — so reading back the path you passed returns the PREVIOUS run's numbers, with no error anywhere |
| `lvai_lunit_scaffold_class_tests` | — (composes `lvai_placeholder_subvi`) | **emits the AIXML for a whole LUnit suite over a class** — one round trip per field plus a defaults and an independence test — and returns the `methodsJson` and `swapsJson` the next two calls need. Fields, types and socket names are derived from the class's own accessors, so none can be misspelled; the VALUES stay the caller's, because a generator that invented them would make six green tests that pin nothing trivially easy. Authoring those six files by hand was 60–90 s of wall clock against 1.7 s inside tools, the largest item left in the route |
| `lvai_generate_vis` | — (composes `lvai_generate_vi` per entry) | **generates several VIs from AIXML in one call**, in order, each with its own optional pane pattern. Sequential on purpose — LabVIEW serialises the RPC — so the saving is round trips. Deletes each AIXML on success and keeps it on failure |
| `lvai_create_class` | — (composes `ValidateAIXML` + `ConvertAIXMLToVI` + `OpenFile` + `RunVIAsTopLevel`) | **creates a `.lvclass`**, its parent link and its private data — by driving LabVIEW's own project provider, because a private data control is compiler output and cannot be built from outside. Needs a project, and opens one |
| `lvai_create_interface` | — (same composition, driving `Add Interface.lvlib:Add Interface to Project (path).vi`) | **creates a LabVIEW interface** — a `.lvclass` with no private data control, which is what enables multiple inheritance. `lvai_create_class`'s `parentInterfaces` links a class to the ones it implements, and only at creation time. Interface *methods* have **no tool of their own**: the route is scriptable and execution-verified, but it is a hand-driven composition written up in `docs/lvclass-interfaces.md` §3 |
| `lvai_add_class_field` | — (composes `ValidateAIXML` + `ConvertAIXMLToVI` + `OpenFile` + `RunVIAsTopLevel`, driving NI's own `Message Maker.lvlib:Add Member Data to Private Data Control.vi`) | **adds private data FIELDS to a class that already exists**, which looked unreachable because `lvai_create_class` only CREATES and its `overwrite` would drop every member. It is the same provider on the same route — it takes a `Target Path` and does not care how old the class is. IT APPENDS: measured 2026-09-17 on a fixture with two fields and four wizard accessors, then on a real 9-member actor class, with every VI still `execState 1` after a project close. A field name the class already carries is refused, because NI's provider would add a second one of that name and nothing downstream reports it. The new field has no accessors — follow with `lvai_create_accessors` passing the reported `nextFromField`, since `-1` resumes from the MEMBER count and that is wrong on a class that also has methods. `docs/lvclass-creation.md` §9 |
| `lvai_create_accessors` | — (same composition, driving NI's own accessor wizard body) | **creates Read/Write VIs** and registers them in the `.lvclass`, saving the library once per field |
| `lvai_add_class_method` | — (composes `ConvertAIXMLToVI` + `pylv_apply` + `RunVIAsTopLevel`) | **turns generated VIs into real class METHODS** — class-typed connector pane, DYNAMIC DISPATCH and class membership — many in one call. This is the step AIXML cannot do at all: a class-typed terminal is `Control with type=UDClassInst is not supported`, so the method is authored with `path` stand-ins, converted *without* validating, and repaired here. It also saves the OWNING CLASS in the same run, without which `Save.Instrument` does not persist the retype, and it verifies from the SAVED FILE with pylabview — because `Execution:State = 1` was measured reporting four healthy methods that had never reached disk |
| `lvai_create_message_class` | — (composes `ConvertAIXMLToVI` + `RunVIAsTopLevel`, driving NI's own Message Maker) | **creates an Actor Framework MESSAGE class** for one public method of an actor — the class, one private data field per parameter, the `Do.vi` override that calls the method, and the `Send <Method>.vi` that enqueues it. It authors NO diagram: a GENERATED override of `Message.lvclass:Do.vi` is `eBad` whatever is on it, measured down to a pass-through with zero nodes, so this clones NI's `Message Template.lvclass` — whose `Do.vi` is already a real override — and runs `Build Do.vi`'s and `Build Send.vi`'s own chains over it. It SAVES THE CLASS at the end, without which every stage reports 0 and both VIs are `eBad` after the next project close. `docs/labview-actor-framework.md` |
| `lvai_add_to_library` | — (composes `ConvertAIXMLToVI` + `OpenFile` + `RunVIAsTopLevel`, driving NI's own `{LV.Library}` `AddItem`) | **puts classes and VIs that already exist into a `.lvlib`**, several in one call. Library membership changes an item's QUALIFIED NAME, and writing `NI.Lib.ContainingLib` by hand does not relink the callers: measured 2026-09-17, the hand edit took all three VIs of a class from `execState 1` to `eBad` while `lvai_describe_class` reported the right `qualifiedName` and `containingLibrary` and both files parsed. `AddItem` plus `Save All This Library.vi` writes both halves and relinks. A `folder` adds INTO a virtual folder — `AddItem` on the FOLDER's project item, which is how NI's Message Maker places a message class — and a folder that is not in the library is refused, because NI's own fallback is to land at the root in silence. It never edits the `.lvproj` — `AddItem` needs the project OPEN and a surviving project edit needs it CLOSED — and names the entries you should remove instead. `docs/labview-actor-framework.md` |
| `lvai_bind_class_fields` | — (composes `ConvertAIXMLToVI` + `OpenFile` + `RunVIAsTopLevel`) | **binds `.ctl` typedefs onto a class's private data fields** — export the cluster, `Replace` each field, move it back — in one call instead of five round trips. Every source is VETTED first and a non-typedef is refused by name: measured on two of NI's own controls, a bind against a non-typedef answers `error out = 0`, installs the right type and binds nothing. Each field is confirmed from the saved class file, not from the helper's answer |
| `lvai_generate_method_test` | — (composes `lvai_generate_vis` + `lvai_generate_vi` + `lvai_swap_subvis`) | **generates a Caraya suite over a class's METHODS**, the companion to `lvai_generate_class_test`'s accessors. Two case shapes: assert the error code a method returns, or write a field, call the method and read it back OFF THE RETURNED OBJECT — which is what proves the class wire threads the method. The method's own error is fed a constant and never chained into Caraya's chain, because a method under test is expected to fail without hardware |
| `lvai_generate_mock_class` | — (composes `ValidateAIXML` + `ConvertAIXMLToVI` + `RunVIAsTopLevel`, and drives Astemes LMock's own `LMock Generate Mock Class.vi`) | **generates an LMock mock class for an INTERFACE** — the `.lvclass`, a constructor, one override per dynamic dispatch member and one `When <Method>.vi` each — without LMock's right-click gesture, which is unreachable from here. Measured against the mock NI ships with the LMock example, the two differ only in uid numbering. **The pre-flight is the point, not the call**: every LMock failure measured arrives as a MODAL DIALOG that stops this whole gRPC service until a human clicks Continue, so a non-interface source is refused HERE from `NI.LVClass.IsInterface` in the file, and `Add to lvproj?` is wired FALSE because LMock's own terminal defaults to TRUE when unwired and then needs an active project. `ok` is judged from the FILES, never from `Created Files` — a refused run was measured naming two paths and writing neither |
| `lvai_dqmh_new_event` | — (composes `ValidateAIXML` + `ConvertAIXMLToVI` + `RunVIAsTopLevel`, and drives Delacor's own `Create New DQMH Event.vi`) | **creates a DQMH request or broadcast** with typed arguments, in one call instead of about fifteen. It drives the DIALOG because `Script New Event.vi` cannot be called from a helper — its `Module Info` holds thirteen refnums and LabVIEW releases them when the parse VI stops, while the dialog runs parse and script as subVIs of one live VI. The module is matched by NAME and confirmed against the dialog's own step-6 text before anything is pressed. **Not unattended-safe**: the OK button is a latched boolean VI Server may not write, so the last step foregrounds the dialog and sends a SPACE |
| `lvai_ensure_labview` | — (process start + service discovery) | **starts LabVIEW** if it is not running, and clears the auto-save store first |

### Without LabVIEW — the pylabview route

These five need no running LabVIEW, no licence and no Python installation: the bundle ships its
own isolated interpreter. They work on a checkout, on a build agent, in CI.

| Tool | What it does |
|---|---|
| `pylv_status` | whether the bundle is present and usable, and which upstream commit it is |
| `pylv_route` | **call this before planning any edit** — decides AIXML vs pylabview for one VI, with the evidence |
| `pylv_extract` | reads a `.vi`, `.ctl` or `.llb` into a directory of XML plus binary sidecars, annotated with primitive and terminal names. Read-only. |
| `pylv_rebuild` | **writes a `.vi`** back from such a bundle |
| `pylv_apply` | one call for a whole edit cycle — close project, extract, apply your operations, rebuild, AIXML-export to verify. Call it with **no operations** first: that mode is read-only and returns the three listings an operations array is written from |

The bundle is **optional and not committed** — about 38 MB — so a fresh checkout has none until
`tools\pylabview\provision.ps1` has run. `pylv_status` says so rather than failing obscurely.

**A release ships it; up to and including v0.9.0 it did not.** The gap was not obvious, because
nothing failed loudly: the `.csproj` only copies `tools\pylabview\runtime\` *if that folder happens
to exist*, no MSBuild target assembles it, and the release workflow ran on a clean checkout where it
never did. So every `pylv_*` tool answered `notProvisioned` on a plugin install and pointed the
reader at `tools\pylabview\provision.ps1` — a path a binary install does not have. Since then the
release workflow assembles the bundle itself and stages it as `bin\pylabview\`, next to the exe
where `PyLabview.Locate()` looks, and fails the release if the interpreter will not import or if a
patch from `patches.json` is missing from the staged copy. The asset is about 38 MB larger for it.

Helper scripts that build on it ship in `scripts\`: `pylv-conpane.py` (repair a connector pane's
pattern without regenerating), `pylv-place-labels.py` (put a diagram comment where you meant it),
`pylv-retarget-subvi.py` (swap which subVI a call points at), `pylv-set-timedloop.py`,
`pylv-decode-terminals.py`.

### Monitors — inverted direction

LabVIEW is the sender here: it pushes a work item when the user triggers an AI feature in the
IDE, and the client answers on the request stream. This is the same hook NI's own
`NigelLocalService` uses.

| Tool | RPC |
|---|---|
| `lvai_monitor_code_completion` | `MonitorCodeCompletion` |
| `lvai_monitor_discuss_vi` | `MonitorDiscussVI` |
| `lvai_monitor_palette_searches` | `MonitorPaletteSearches` |
| `lvai_monitor_example_searches` | `MonitorExampleSearches` |
| `lvai_monitor_front_panel_cleanup` | `MonitorFrontPanelCleanup` |
| `lvai_monitor_project_changes` | `MonitorProjectChanges` |

